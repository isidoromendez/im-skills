# Historial de Iteraciones Globales — {nombre_proyecto}

> Este archivo es de solo-anexado (append-only). Registra únicamente eventos a
> nivel de iteración GLOBAL (inicio/cierre de cada corrida completa de las 4
> fases), no el detalle de cada fase — eso vive en `iter-N/audit.md`. Nunca se
> edita ni se borra una entrada existente.

---

## [__PENDIENTE_TIMESTAMP__] — ITERACION_GLOBAL_INICIADA

- **Fase / Iteración global:** iter-0
- **Evento:** Inicialización de la primera iteración global de la investigación.
- **Detalle:** Se crea `proyecto.json` y `iter-0/`. Sin insumo de iteración anterior (es la primera).
- **Aprobado por usuario:** N/A (evento de sistema)

---
